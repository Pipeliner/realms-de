# Native format: realm and its packaging live in one repository and there is no
# separate upstream tarball to track. Revisit if realm is ever uploaded to
# Debian proper, which expects a non-native package with an orig tarball.
extend-diff-ignore = "^(target|result)/"
